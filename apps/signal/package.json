{
  "name": "@amida/signal",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/server.ts",
    "build": "tsc -p tsconfig.json && wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@amida/protocol": "file:../../packages/protocol"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260312.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.11.1"
  }
}
